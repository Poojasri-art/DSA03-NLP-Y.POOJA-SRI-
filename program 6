import random
from nltk.util import bigrams
from collections import defaultdict

text = "I love natural language processing because natural language is interesting"

words = text.split()

bigram_model = defaultdict(list)

for w1, w2 in bigrams(words):
    bigram_model[w1].append(w2)

current = "I"
generated = [current]

for i in range(9):
    next_words = bigram_model.get(current)

    if not next_words:
        break

    current = random.choice(next_words)
    generated.append(current)

print("Generated Text:")
print(" ".join(generated))





OUTPUT:
Generated Text:
I love natural language processing because natural language is interesting
